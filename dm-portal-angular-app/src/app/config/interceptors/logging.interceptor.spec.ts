import { Logging.Interceptor } from './logging.interceptor';

describe('Logging.Interceptor', () => {
  it('should create an instance', () => {
    expect(new Logging.Interceptor()).toBeTruthy();
  });
});
